import { AuthUserSignUpRepository } from "./@types/auth-user.types";

export class AuthUserRepository {
  async SignUp({ username, email, password }: AuthUserSignUpRepository) {
    try {
      
    } catch (error) {}
  }
}
